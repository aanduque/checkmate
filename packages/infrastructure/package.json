{
  "name": "@checkmate/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'Build complete (Bun runs TypeScript directly)'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@checkmate/domain": "workspace:*",
    "@checkmate/application": "workspace:*",
    "rrule": "^2.8.1",
    "filtrex": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
